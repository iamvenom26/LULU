<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Server Development Academy | LULU Learning</title>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style> 
        :root {
    --primary-color: #2c3e50;
    --secondary-color: #34495e;
    --accent-color: #27ae60;
    --background-color: #1a1a2e;
    --card-bg: #1f2937;
    --text-color: #ffffff;
    --text-secondary: #e0e0e0;
    --success-color: #2ecc71;
    --warning-color: #f1c40f;
    --error-color: #e74c3c;
    --border-radius: 10px;
    --transition: all 0.3s ease;
}

/* Global Styles */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Poppins', sans-serif;
    line-height: 1.6;
    background: var(--background-color);
    color: var(--text-color);
}

.container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 20px;
}

/* Navigation */
.navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    background: rgba(26, 26, 46, 0.95);
    backdrop-filter: blur(10px);
    z-index: 1000;
    padding: 1rem 0;
    border-bottom: 1px solid rgba(39, 174, 96, 0.1);
}

.nav-container {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 2rem;
}

.nav-left {
    display: flex;
    align-items: center;
    gap: 2rem;
}

.logo {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--accent-color);
}

.back-home {
    display: flex;
    align-items: center;
    gap: 8px;
    color: var(--text-color);
    text-decoration: none;
    font-weight: 500;
    transition: var(--transition);
}

.back-home:hover {
    color: var(--accent-color);
}

.enroll-btn {
    padding: 0.5rem 1.5rem;
    background: var(--accent-color);
    color: var(--text-color);
    border-radius: 25px;
    text-decoration: none;
    transition: var(--transition);
}

.enroll-btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 5px 15px rgba(39, 174, 96, 0.3);
}

/* Hero Section */
.hero {
    padding: 8rem 2rem 4rem;
    background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
    text-align: center;
    position: relative;
}

.hero-content {
    max-width: 800px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
}

.hero-badge {
    display: inline-block;
    background: rgba(39, 174, 96, 0.2);
    color: var(--accent-color);
    padding: 0.5rem 1rem;
    border-radius: 20px;
    margin-bottom: 1rem;
    font-weight: 500;
}

.hero h1 {
    font-size: 3.5rem;
    margin-bottom: 1rem;
    line-height: 1.2;
}

.hero-subtitle {
    font-size: 1.2rem;
    color: var(--text-secondary);
    margin-bottom: 2rem;
}

/* Hero Stats */
.hero-stats {
    display: flex;
    justify-content: center;
    gap: 3rem;
    margin-top: 3rem;
}

.stat-item {
    display: flex;
    align-items: center;
    gap: 1rem;
}

.stat-item i {
    font-size: 2rem;
    color: var(--accent-color);
}

.stat-info {
    text-align: left;
}

.stat-number {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--text-color);
}

.stat-label {
    font-size: 0.9rem;
    color: var(--text-secondary);
}
/* Advertisement Banner */
.ad-banner {
    background: linear-gradient(45deg, #2c3e50, #3498db);
    padding: 1.5rem 0;
}

.ad-content {
    text-align: center;
}

.ad-badge {
    background: #e74c3c;
    color: white;
    padding: 0.3rem 0.8rem;
    border-radius: 15px;
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
    display: inline-block;
}

.countdown {
    color: #ffd700;
    font-weight: bold;
}

/* Course Categories */
.course-categories {
    padding: 1rem 0;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
}

.category-btn {
    background: none;
    border: 1px solid var(--text-secondary);
    color: var(--text-secondary);
    padding: 0.5rem 1.5rem;
    margin: 0 0.5rem;
    border-radius: 20px;
    cursor: pointer;
    transition: var(--transition);
}

.category-btn.active,
.category-btn:hover {
    background: var(--accent-color);
    border-color: var(--accent-color);
    color: white;
}

/* Courses Section */
.courses {
    padding: 3rem 0;
}

.courses h2 {
    font-size: 2rem;
    margin-bottom: 2rem;
}

.course-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
}

.course-card {
    background: var(--card-bg);
    border-radius: 8px;
    overflow: hidden;
    transition: var(--transition);
    border: 1px solid rgba(255, 255, 255, 0.1);
}

.course-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
}

.course-image {
    position: relative;
    aspect-ratio: 16/9;
}

.course-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
}

.course-content {
    padding: 1rem;
}

/* Fallback ellipsis for older browsers */
@supports not (-webkit-line-clamp: 2) {
    .course-title::after {
        content: '...';
        position: absolute;
        right: 0;
        bottom: 0;
        background: var(--card-bg);
        padding-left: 4px;
    }
}

.instructor {
    color: var(--text-secondary);
    font-size: 0.9rem;
    margin-bottom: 0.5rem;
}

.rating {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    margin-bottom: 0.5rem;
}

.rating-score {
    color: #f1c40f;
    font-weight: bold;
}

.stars {
    color: #f1c40f;
}

.rating-count {
    color: var(--text-secondary);
    font-size: 0.9rem;
}

.meta-info {
    color: var(--text-secondary);
    font-size: 0.8rem;
    margin-bottom: 0.5rem;
}

.price-info {
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.current-price {
    font-size: 1.2rem;
    font-weight: bold;
}

.original-price {
    color: var(--text-secondary);
    text-decoration: line-through;
    font-size: 0.9rem;
}

.bestseller-tag,
.hot-tag {
    display: inline-block;
    padding: 0.2rem 0.5rem;
    border-radius: 4px;
    font-size: 0.8rem;
    font-weight: 600;
    margin-top: 0.5rem;
}

.bestseller-tag {
    background: #ffd700;
    color: #000;
}

.hot-tag {
    background: #e74c3c;
    color: white;
}

/* Featured Topics */
.featured-topics {
    padding: 3rem 0;
    background: rgba(255, 255, 255, 0.02);
}

.topics-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1.5rem;
    margin-top: 2rem;
}

.topic-card {
    background: var(--card-bg);
    padding: 2rem;
    border-radius: 8px;
    text-align: center;
    transition: var(--transition);
}

.topic-card:hover {
    transform: translateY(-5px);
}

.topic-card i {
    font-size: 2.5rem;
    color: var(--accent-color);
    margin-bottom: 1rem;
}

/* Footer */
.footer {
    background: var(--card-bg);
    padding: 2rem 0;
    margin-top: 3rem;
}

.footer-content {
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.footer-links {
    display: flex;
    gap: 2rem;
}

.footer-links a {
    color: var(--text-secondary);
    text-decoration: none;
    transition: var(--transition);
}

.footer-links a:hover {
    color: var(--accent-color);
}

/* Responsive Design */
@media (max-width: 1200px) {
    .course-grid,
    .topics-grid {
        grid-template-columns: repeat(3, 1fr);
    }
}

@media (max-width: 992px) {
    .course-grid,
    .topics-grid {
        grid-template-columns: repeat(2, 1fr);
    }
    
    .hero h1 {
        font-size: 2.5rem;
    }
    
    .hero-stats {
        flex-direction: column;
        gap: 1.5rem;
    }
}

@media (max-width: 768px) {
    .nav-container {
        padding: 0 1rem;
    }
    
    .footer-content {
        flex-direction: column;
        gap: 1rem;
        text-align: center;
    }
    
    .footer-links {
        flex-wrap: wrap;
        justify-content: center;
    }
}

@media (max-width: 576px) {
    .course-grid,
    .topics-grid {
        grid-template-columns: 1fr;
    }
    
    .hero h1 {
        font-size: 2rem;
    }
    
    .category-btn {
        margin: 0.25rem;
    }
}
    </style>
  
</head>
<body>
    <!-- Navigation -->
    <nav class="navbar">
        <div class="nav-container">
            <div class="nav-left">
                <a href="/" class="back-home">
                    <i class="fas fa-home"></i> Back to Home
                </a>
                <div class="logo">LULU Academy</div>
            </div>
            <div class="nav-right">
                <% if (locals.user) { %>
                    <div class="nav-right">
                        <a href="#" class="enroll-btn">Enroll Now</a>
                    </div>
                    <% } else { %>
                        <div class="nav-left">
                            <a href="/user/signin" class="enroll-btn">Login</a>
                        </div>
                    <% } %>
               
                
            </div>
        </div>
    </nav>

    <!-- Hero Section -->
    <header class="hero">
        <div class="hero-content">
            <div class="hero-badge">Server Development 2024</div>
            <h1>Master Server-Side Technologies</h1>
            <p class="hero-subtitle">Build robust backend systems and server architectures</p>
            <div class="hero-stats">
                <div class="stat-item">
                    <i class="fas fa-server"></i>
                    <div class="stat-info">
                        <span class="stat-number">100K+</span>
                        <span class="stat-label">Active Students</span>
                    </div>
                </div>
                <div class="stat-item">
                    <i class="fas fa-star"></i>
                    <div class="stat-info">
                        <span class="stat-number">4.8</span>
                        <span class="stat-label">Course Rating</span>
                    </div>
                </div>
                <div class="stat-item">
                    <i class="fas fa-certificate"></i>
                    <div class="stat-info">
                        <span class="stat-number">100%</span>
                        <span class="stat-label">Job Assistance</span>
                    </div>
                </div>
            </div>
        </div>
    </header>

    <!-- Advertisement Banner -->
    <div class="ad-banner">
        <div class="container">
            <div class="ad-content">
                <span class="ad-badge">Limited Time Offer</span>
                <h3>Get 75% Off on All Server Development Courses</h3>
                <p>Offer ends in: <span class="countdown">2d 15h 30m</span></p>
            </div>
        </div>
    </div>

    <!-- Course Categories -->
    <div class="course-categories">
        <div class="container">
            <button class="category-btn active">All</button>
            <button class="category-btn">Database</button>
            <button class="category-btn">Web Servers</button>
            <button class="category-btn">APIs</button>
        </div>
    </div>
      <!-- Courses Section -->
    <section id="courses" class="courses">
        <div class="container">
            <h2>Trending Frontend Courses</h2>
            <div class="course-grid">
                
                <!-- Dynamic Courses from Database -->
                <% if (courses && courses.length > 0) { %>
                    <% courses.filter(course => course.category === "Server Management").forEach(course => { %>
                        <form action="/videos/<%= course._id %>" method="get">
                            <button type="submit" style="all: unset; cursor: pointer; display: block; width: 100%;">
                                <div class="course-card">
                                    <div class="course-image">
                                        <img src="<%= course.coverProfile %>" alt="<%= course.title %>">
                                    </div>
                                    <div class="course-content">
                                        <h3 class="course-title"><%= course.title %></h3>
                                        <p class="instructor"><%= course.authorName %></p>
                
                                        <div class="rating">
                                            <span class="rating-score">4.9</span>
                                            <span class="stars">★★★★★</span>
                                            <span class="rating-count">(18,521)</span>
                                        </div>
                                        <div class="meta-info">
                                            <span>38 hours</span> • <span>450 lectures</span>
                                        </div>
                                        <div class="price-info">
                                            <span class="current-price">₹<%= course.price %></span>
                                            <span class="original-price">₹3,999</span>
                                        </div>
                                        <span class="bestseller-tag">Bestseller</span>
                                    </div>
                                </div>
                            </button>
                        </form>
                    <% }); %>
                <% } else { %>
                    <p>No courses available</p>
                <% } %>
                
    
                <!-- Static React Course -->
                
    
            </div>
        </div>
    </section>
        
        <!-- Footer -->
        <footer class="footer">
            <div class="container">
                <div class="footer-content">
                    <p>&copy; 2024 LULU Academy. All rights reserved.</p>
                    <div class="footer-links">
                        <a href="#">Terms</a>
                        <a href="#">Privacy policy</a>
                        <a href="#">Cookie settings</a>
                        <a href="#">Sitemap</a>
                    </div>
                </div>
            </div>
        </footer>
    
       
    </body>
    </html>
