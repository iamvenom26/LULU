<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin - Upload Video</title>
    <style>
        body { font-family: Arial, sans-serif; margin: 20px; }
        .container { max-width: 600px; margin: auto; }
        .video-list { margin-top: 20px; }
        .video-item { display: flex; justify-content: space-between; padding: 10px; border: 1px solid #ddd; margin-top: 5px; }
    </style>
</head>
<body>
    <div class="container">
        <h2>Upload Video</h2>
        <form action="/admin/upload" method="POST" enctype="multipart/form-data">
            <input type="text" name="title" placeholder="Video Title" required><br><br>
            <input type="text" name="authorName" placeholder="Author Name" required><br><br>
            <input type="number" name="price" placeholder="Price" required><br><br>
            <textarea name="description" placeholder="Description" required></textarea><br><br>
            <input type="date" name="publishDate"><br><br>
            <input type="file" name="video" accept="video/*" required><br><br>
            <button type="submit">Upload</button>
        </form>
        
        <h2>Uploaded Videos</h2>
        <div class="video-list">
            <% videos.forEach(video => { %>
                <div class="video-item">
                    <span><%= video.title %> by <%= video.authorName %> - $<%= video.price %></span>
                    <form action="/videos/<%= video._id %>?_method=DELETE" method="POST" style="display:inline;">
                        <button type="submit">Delete</button>
                    </form>
                </div>
            <% }) %>
        </div>
    </div>
</body>
</html>