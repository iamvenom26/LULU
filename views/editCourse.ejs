<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Course - <%= course.title %></title>
    <link
      href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap"
      rel="stylesheet"
    />
    <style>
      /* General Reset */
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      body {
        font-family: 'Poppins', sans-serif;
        background: linear-gradient(135deg, #0a192f, #1e3a8a);
        color: #ffffff;
        padding: 20px;
        margin: 0;
      }
      /* Top Navigation */
      .top-nav {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 20px;
      }
      .back-home {
        color: #dbeafe;
        font-size: 16px;
        text-decoration: none;
        font-weight: bold;
        padding: 10px 15px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 5px;
        transition: background 0.3s ease;
      }
      .back-home:hover {
        background: rgba(255, 255, 255, 0.2);
      }
      .page-title {
        font-size: 2rem;
        letter-spacing: 1px;
      }
      /* Container */
      .container {
        width: 90%;
        max-width: 1200px;
        margin: auto;
        background: rgba(255, 255, 255, 0.1);
        padding: 20px;
        border-radius: 12px;
        box-shadow: 0 0 20px rgba(255, 255, 255, 0.2);
        backdrop-filter: blur(10px);
        text-align: left;
      }
      /* Edit Form */
      .edit-container {
        display: flex;
        flex-wrap: wrap;
        gap: 20px;
        margin-bottom: 20px;
      }
      .cover-section {
        flex: 1 1 300px;
        text-align: center;
      }
      .cover-section img {
        max-width: 100%;
        border-radius: 8px;
        border: 2px solid #ffffff;
        margin-bottom: 15px;
      }
      .details-section {
        flex: 2 1 400px;
      }
      .course-detail {
        display: flex;
        align-items: center;
        padding: 10px 0;
        border-bottom: 1px solid rgba(255, 255, 255, 0.3);
      }
      .course-detail label {
        flex: 1;
        font-weight: bold;
        margin-right: 10px;
      }
      .course-detail input,
      .course-detail textarea {
        flex: 2;
        background: transparent;
        border: none;
        color: #ffffff;
        font-size: 16px;
        outline: none;
      }
      .edit-btn {
        cursor: pointer;
        background: none;
        border: none;
        color: #dbeafe;
        font-size: 16px;
        margin-left: 10px;
      }
      .edit-btn:hover {
        color: #ffffff;
      }
      button[type='submit'] {
        margin-top: 20px;
        padding: 10px 20px;
        background: #1e3a8a;
        color: #ffffff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }
      button[type='submit']:hover {
        transform: scale(1.05);
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.5);
      }
      /* Video Section */
      .video-section {
        margin-top: 30px;
      }
      .video-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 20px;
        margin-top: 20px;
      }
      .video-card {
        background: rgba(255, 255, 255, 0.1);
        padding: 15px;
        border-radius: 8px;
        text-align: center;
        box-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
      }
      .video-card img {
        width: 100%;
        border-radius: 8px;
        border: 2px solid #ffffff;
        margin-bottom: 10px;
      }
      .video-card input[type='text'] {
        width: 100%;
        padding: 8px;
        margin-bottom: 10px;
        border-radius: 4px;
        border: 1px solid #ccc;
        font-size: 14px;
      }
      .video-card input[type='file'] {
        width: 100%;
        margin-bottom: 10px;
      }
      .video-card .btn {
        display: block;
        margin: 10px auto;
        width: calc(100% - 20px);
        text-align: center;
        background: #4976d6;
        color: #fff;
        padding: 10px 0;
        border-radius: 5px;
        transition: background 0.3s ease;
        text-decoration: none;
        font-weight: bold;
      }
      .video-card .btn:hover {
        background: #355ecf;
      }
      .video-card .delete-btn {
        background: #ff4444;
        color: #fff;
        padding: 8px 12px;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        margin-top: 5px;
        font-weight: bold;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }
      .video-card .delete-btn:hover {
        transform: scale(1.05);
        box-shadow: 0 0 15px rgba(255, 0, 0, 0.7);
      }
      /* Add New Videos Form */
      .new-video-form {
        margin-top: 30px;
      }
      .new-video-form label {
        font-weight: bold;
      }
      .new-video-form input[type='file'] {
        width: 100%;
        margin-bottom: 10px;
      }
      .new-video-form button {
        width: 100%;
        padding: 10px;
        background: #1e3a8a;
        color: #ffffff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
        font-size: 16px;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }
      .new-video-form button:hover {
        transform: scale(1.05);
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.5);
      }
      /* Back Button */
      .back-btn {
        display: inline-block;
        margin-top: 20px;
        padding: 10px 20px;
        background: #1e3a8a;
        color: #ffffff;
        text-decoration: none;
        border-radius: 5px;
        font-size: 16px;
        font-weight: bold;
        transition: transform 0.2s ease, box-shadow 0.2s ease;
      }
      .back-btn:hover {
        transform: scale(1.05);
        box-shadow: 0 0 15px rgba(255, 255, 255, 0.5);
      }
    </style>
    <script>
      function enableEdit(fieldId) {
        document.getElementById(fieldId).removeAttribute('readonly');
        document.getElementById(fieldId).focus();
      }
    </script>
  </head>
  <body>
    <!-- Top Navigation -->
    <div class="top-nav">
      <a href="/videos/all" class="back-home">Back to Courses</a>
      <h1 class="page-title">Edit Course</h1>
      <div></div>
    </div>

    <div class="container">
      <!-- Edit Course Details Form -->
      <form
        action="/videos/edit/<%= course._id %>"
        method="POST"
        enctype="multipart/form-data"
      >
        <div class="edit-container">
          <div class="cover-section">
            <label>Current Cover Image:</label><br />
            <% if (course.coverProfile) { %>
              <img
                src="<%= course.coverProfile.startsWith('/') ? course.coverProfile : '/uploads/covers/' + course.coverProfile %>"
                alt="Cover Image"
              />
            <% } else { %>
              <img src="/default-cover.jpg" alt="No Cover Available" />
            <% } %>
            <br />
            <label>Upload New Cover Image:</label>
            <input type="file" name="coverProfile" accept="image/*" />
          </div>

          <div class="details-section">
            <div class="course-detail">
              <label>Title:</label>
              <input
                type="text"
                id="title"
                name="title"
                value="<%= course.title %>"
                readonly
              />
              <button
                type="button"
                class="edit-btn"
                onclick="enableEdit('title')"
              >
                ✏️
              </button>
            </div>
            <div class="course-detail">
              <label>Author Name:</label>
              <input
                type="text"
                id="authorName"
                name="authorName"
                value="<%= course.authorName %>"
                readonly
              />
              <button
                type="button"
                class="edit-btn"
                onclick="enableEdit('authorName')"
              >
                ✏️
              </button>
            </div>
            <div class="course-detail">
              <label>Price:</label>
              <input
                type="number"
                id="price"
                name="price"
                value="<%= course.price %>"
                readonly
              />
              <button
                type="button"
                class="edit-btn"
                onclick="enableEdit('price')"
              >
                ✏️
              </button>
            </div>
            <div class="course-detail">
              <label>Description:</label>
              <textarea id="description" name="description" readonly>
<%= course.description %>
              </textarea>
              <button
                type="button"
                class="edit-btn"
                onclick="enableEdit('description')"
              >
                ✏️
              </button>
            </div>
          </div>
        </div>
        <button type="submit">Update Course</button>
      </form>

      <!-- Existing Videos Section -->
      <div class="video-section">
        <h2>Existing Videos</h2>
        <% if (course.videos && course.videos.length > 0) { %>
          <div class="video-grid">
            <% course.videos.forEach((video) => { %>
              <div class="video-card">
                <form action="/videos/edit/<%= course._id %>/update-video/<%= video._id %>" method="POST" enctype="multipart/form-data">
                  <input type="hidden" name="courseId" value="<%= course._id %>">
                <img
                  src="<%= video.coverProfile ? (video.coverProfile.startsWith('/') ? video.coverProfile : '/uploads/covers/' + video.coverProfile) : '/uploads/videos/default-thumbnail.jpg' %>"
                  alt="<%= video.title %>"
                />
                  
                   <input type="text" name="videoTitle-<%= video._id %>" value="<%= video.title %>" placeholder="Enter video title" style="width:100%; padding:5px; margin:5px 0;" />

                  <label>Update Video Cover:</label>
                  <input type="file" name="videoCover-<%= video._id %>" accept="image/*" style="width:100%;" />

                  <button type="submit">Update Video</button>
                </form>


                <a href="/videos/watch/<%= video._id %>" class="btn"
                  >Watch Video</a
                >
                <!-- Delete Video Form -->
                <form
                  action="/videos/edit/<%= course._id %>/delete-video/<%= video._id %>"
                  method="POST"
                  style="margin-top: 10px;"
                >
                  <input type="hidden" name="courseId" value="<%= course._id %>" />
                  <button type="submit" class="delete-btn">Delete</button>
                </form>
              </div>
            <% }); %>
          </div>
        <% } else { %>
          <p>No videos available.</p>
        <% } %>
      </div>

      <!-- Add New Videos Section -->
      <div class="new-video-form">
        <h2>Add New Videos</h2>
        <form
          action="/videos/edit/<%= course._id %>/add-videos"
          method="POST"
          enctype="multipart/form-data"
        >
          <label>Select Video Files:</label>
          <input
            type="file"
            name="videos"
            accept="video/mp4,video/mkv,video/avi"
            multiple
            required
          />
          <button type="submit">Upload Videos</button>
        </form>
      </div>

      <a href="/videos/all" class="back-btn">Back to Courses</a>
    </div>
  </body>
</html>
