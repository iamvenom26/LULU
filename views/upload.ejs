<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Video</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your CSS file -->
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #0072ff, #00c6ff); /* Blue gradient */
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.95); /* Semi-transparent white */
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            width: 500px;
            max-width: 90%;
            animation: slideIn 1s ease-out;
        }

        @keyframes slideIn {
            0% {
                transform: translateY(-100px);
                opacity: 0;
            }
            100% {
                transform: translateY(0);
                opacity: 1;
            }
        }

        h2 {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 20px;
            color: #0072ff; /* Blue text */
        }

        .form-row {
            display: flex;
            align-items: center;
            margin-bottom: 15px;
        }

        .form-row label {
            flex: 1;
            font-weight: bold;
            color: #0072ff; /* Blue text */
            margin-right: 10px;
        }

        .form-row input,
        .form-row textarea,
        .form-row select {
            flex: 2;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-row input:focus,
        .form-row textarea:focus,
        .form-row select:focus {
            border-color: #0072ff; /* Blue border on focus */
            outline: none;
            box-shadow: 0 0 5px rgba(0, 114, 255, 0.5);
        }

        .form-row textarea {
            resize: vertical;
            height: 100px;
        }

        .form-row input[type="file"] {
            padding: 8px;
            background: #f9f9f9;
        }

        button {
            width: 100%;
            padding: 10px;
            background: linear-gradient(135deg, #0072ff, #00c6ff); /* Blue gradient */
            color: #fff;
            font-weight: bold;
            cursor: pointer;
            border: none;
            border-radius: 5px;
            transition: transform 0.3s ease, background 0.3s ease;
        }

        button:hover {
            transform: scale(1.05);
            background: linear-gradient(135deg, #00c6ff, #0072ff); /* Reverse gradient on hover */
        }

        a {
            display: block;
            text-align: center;
            margin-top: 20px;
            color: #0072ff; /* Blue text */
            text-decoration: none;
            font-weight: bold;
            transition: color 0.3s ease;
        }

        a:hover {
            color: #00c6ff; /* Lighter blue on hover */
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>Upload Course</h2>
        <form action="/videos/upload" method="POST" enctype="multipart/form-data">
            <!-- Row 1: Course Title -->
            <div class="form-row">
                <label for="title">Course Title:</label>
                <input type="text" id="title" name="title" required>
            </div>

            <!-- Row 2: Author Name -->
            <div class="form-row">
                <label for="authorName">Author Name:</label>
                <input type="text" id="authorName" name="authorName" required>
            </div>

            <!-- Row 3: Price -->
            <div class="form-row">
                <label for="price">Price:</label>
                <input type="number" id="price" name="price" required>
            </div>

            <!-- Row 4: Description -->
            <div class="form-row">
                <label for="description">Description:</label>
                <textarea id="description" name="description" required></textarea>
            </div>

            <!-- Row 5: Cover Profile Image -->
            <div class="form-row">
                <label for="coverProfile">Cover Profile Image:</label>
                <input type="file" id="coverProfile" name="coverProfile" accept="image/*">
            </div>

            <!-- Row 6: Upload Videos -->
            <div class="form-row">
                <label for="videos">Upload Videos:</label>
                <input type="file" id="videos" name="videos" accept="video/mp4,video/mkv,video/avi" multiple required>
            </div>

            <!-- Row 7: Category -->
            <div class="form-row">
                <label for="category">Category:</label>
                <select name="category" required>
                    <option value="Frontend">Frontend</option>
                    <option value="Backend">Backend</option>
                    <option value="Server Management">Server Management</option>
                </select>
            </div>

            <!-- Submit Button -->
            <button type="submit">Upload</button>
        </form>
        <a href="/videos/all">Back to All Videos</a>
    </div>
</body>
</html>